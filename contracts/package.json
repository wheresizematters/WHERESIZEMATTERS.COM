{
  "name": "size-staking-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "deploy:testnet": "hardhat run deploy/001_deploy_staking.ts --network baseSepolia",
    "deploy:mainnet": "hardhat run deploy/001_deploy_staking.ts --network base",
    "verify": "hardhat verify --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.4.0"
  }
}
